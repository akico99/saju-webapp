# DESIGN.md — 사주보는 수달

웹(입력·결과 화면)과 PDF(리포트)가 같은 언어를 쓰기 위한 기준. 새 페이지·새 템플릿을 만들 때
이 파일을 먼저 읽고, 여기 없는 색·그림자·모서리는 만들지 않는다. 토큰의 실제 값은
`public/theme.css`(웹)와 `src/pdf/templates/report.css`(PDF)에 있고, 이 문서는 "왜 그렇게
쓰는가"와 "무엇을 하지 않는가"를 적는다.

## 1. 분위기

따뜻한 종이 위에 남색 잉크로 쓴 편지. 계산기처럼 차갑지도, 무속 서비스처럼 어둡지도 않다.
참고한 방향: claude.com(크림 캔버스 + 세리프 헤드라인 + 어두운 카드로 페이싱),
Notion(헤어라인 + 거의 없는 그림자), WIRED(종이 신문의 구분선과 세리프 사다리),
Mastercard(챕터 오프너의 아이브로우와 고스트 워터마크).

핵심 규칙 네 가지:
- 바닥은 항상 크림. 순백은 "종이 한 장"(입력란·카드)에만 쓴다.
- 구조색은 남색 하나. 금색은 강조 포인트(아이브로우·별·핵심 숫자)에만, 한 화면에 세 곳 이하.
- 깊이는 헤어라인 1px로 만든다. 그림자는 거의 안 보이는 정도로만.
- 제목은 세리프, 본문은 산세리프, 메타 정보는 작은 산세리프 대문자 트래킹.

## 2. 색

| 역할 | 웹 토큰 | 값 | 쓰는 곳 |
|---|---|---|---|
| 바닥(크림) | `--bg` | `#FFF9EF` | 앱 기둥 바닥, 결과 페이지 배경 |
| 바깥 크림 | `--bg-outer` | `#F3EAD9` | PC에서 기둥 바깥 |
| 종이 | `--paper` / `--surface` | `#FFFFFF` | 입력란, 카드, 타일 |
| 안쪽 면 | `--surface-2` | `#F5EDE0` | 카드 안의 블록, 막대 트랙, 비활성 버튼 |
| 잉크(남색) | `--ink` / `--accent` | `#17243D` | 본문, 제목, 주요 버튼, 다크 카드 배경 |
| 보조 글자 | `--muted-2` | `#76716A` | 안내문, 캡션 (크림 위 4.6:1 이상 유지) |
| 헤어라인 | `--line` | `#EEE5D8` | 카드 테두리, 구분선 |
| 금 | `--gold` | `#D5A542` | 아이브로우, 별, 핵심 숫자, 포커스 링 |
| 좋음 | `--success` | `#2F6A5E` | DO 목록, 좋은 날 |
| 주의 | `--danger` | `#A3513A` | DON'T 목록, 충 |

PDF는 같은 계열의 인쇄용 값을 쓴다: 종이 `#faf8f4`, 잉크 `#24344b`, 본문 `#354457`,
금 `#937440`(종이 위) / `#e0c78d`(남색 위), 헤어라인 `#dedfd9`. 표지만 남색 `#24344b`
전면, 본문 페이지는 전부 종이색이다.

하지 않는 것:
- 초록 계열 강조(예전 `#7a947f`, `#edf2ec`)는 쓰지 않는다. 좋음/나쁨 의미색 외의 초록은 없다.
- 파랑·보라 계열은 없다.
- 그라데이션은 배너 오버레이와 주요 버튼(`--accent-grad`)에만.

## 3. 타이포그래피

| 역할 | 서체 | 크기(웹 / PDF) | 굵기 | 트래킹 |
|---|---|---|---|---|
| 페이지 제목 | Noto Serif KR / ReportMyeongjo | 26–30px / 24pt | 700 / 400 | −0.01em |
| 섹션 제목(h2) | Noto Serif KR / ReportMyeongjo | 20px / 17–19pt | 700 | 0 |
| 소제목(h3.sub) | Noto Serif KR / ReportMyeongjo | 16px / 12.5pt | 700 | 0 |
| 본문 | Pretendard / ReportGothic | 15px / 11.3pt | 400 | 0 |
| 캡션·안내 | Pretendard / ReportGothic | 12–13px / 9pt | 400 | 0 |
| 아이브로우·메타 | Pretendard / ReportGothic | 11–12px / 8.5–9.5pt | 700 | +0.12–0.22em |

- 세리프는 "읽는 제목"에만. 버튼·라벨·표에는 세리프를 쓰지 않는다.
- 아이브로우는 항상 금색 + 트래킹 + 앞에 작은 점(·) 또는 별. 예: `· 第 03 章`, `· 앞으로 7일`.
- 본문 줄간격 1.65(웹) / 1.78(PDF). 문단 사이는 줄간격의 절반 정도(웹 12px / PDF 5mm).
- 강조는 `<mark>`(형광펜 `#eee4ca`) 한 종류만, 한 챕터에 2~5곳.

## 4. 깊이와 모서리

세 단계만 있다:

| 단계 | 처리 | 쓰는 곳 |
|---|---|---|
| 0 평면 | 헤어라인 `1px solid var(--line)`, 그림자 없음 | 기본 카드, 입력란, 표 |
| 1 살짝 | `--shadow-1`: `0 1px 2px rgba(60,40,20,.04), 0 4px 18px rgba(60,40,20,.05)` | 결과 카드, 폼 컨테이너 |
| 2 떠 있음 | `--shadow-2`: `0 8px 24px rgba(23,36,61,.10), 0 24px 56px rgba(23,36,61,.08)` | 서랍, 바텀시트, 고정 CTA |

- 그림자는 항상 잉크색(남색 또는 갈색)을 띄고 낮은 알파. 검정 `rgba(0,0,0,.18)`은 쓰지 않는다.
- 단계 2는 화면에 동시에 하나만.
- 주요 버튼의 `--accent-glow`(금색 번짐)는 그림자가 아니라 강조이므로 단계에 세지 않는다.

모서리:

| 토큰 | 값 | 쓰는 곳 |
|---|---|---|
| `--radius-card` | 20px | 카드, 폼 컨테이너 |
| `--radius-btn` | 12px | 버튼, 입력란, 세그먼트 |
| `--radius-pill` | 999px | 칩, 키워드, 배지 |
| PDF | 0–3mm | 인쇄물은 각지게. 카드는 3mm, 소제목·표는 0 |

## 5. 컴포넌트 규칙

- **카드**: 종이 배경 + 헤어라인 + 단계 0 또는 1. 카드 안에 카드를 넣지 않는다(안쪽은 `--surface-2` 블록).
- **다크 카드**: 남색 바닥 + 크림 글자 + 금 아이브로우. 한 페이지에 한 장만("명식 한 장", 점수 요약). 페이지의 페이싱을 만드는 장치이지 장식이 아니다.
- **주요 버튼**: 남색 바닥 + 흰 글자, 12px 모서리, 높이 52~58px. 페이지에 하나.
- **보조 버튼**: 종이 바닥 + 헤어라인 + 남색 글자.
- **세그먼트(선택지)**: 종이 알약, 선택되면 남색 반전. 2열(폰) / 4열(PC).
- **입력란**: 종이 바닥 + 헤어라인, 포커스 시 금색 링 `0 0 0 3px var(--gold-soft)`.
- **DO / DON'T**: 좋음·주의 의미색만 쓰고, 아이콘 없이 색 점으로 구분.
- **대화형 입력(스테이지)**: 전체 화면 그림 + 말풍선 질문 한 개 + 답 하나. 한 화면에 질문 하나.

## 6. PDF 챕터 규칙

- **표지**: 남색 전면, 금 아이브로우, 세리프 제목, 명식 요약. 유일한 다크 페이지.
- **챕터 오프너**: 종이 위에 챕터 번호를 크림-온-크림 고스트 워터마크(약 64pt, `#f1ede4`)로
  깔고, 그 위에 금 아이브로우(`· 第 03 章`) → 세리프 h2 → 헤어라인. 키워드 칩은 헤어라인 아래.
- **소제목(h3.sub)**: 왼쪽 세로선 대신 아래 헤어라인. 각진 모서리. 위 여백 8mm, 아래 3mm.
- **표·데이터**: 산세리프, 헤더는 아이브로우 스타일, 행 구분은 헤어라인. 배경색 칠하지 않는다.
- **할 것 / 피할 것**: 두 열, 의미색 점 + 본문색 글자. 상자 배경은 종이보다 한 단계만 어둡게.
- **면책·출처**: 9pt, `--muted`, 마지막 페이지 하단.

## 7. 하지 않는 것 (요약)

- 검정 그림자, 두꺼운 그림자, 카드 위 카드
- 초록·파랑·보라 강조색
- 세리프 버튼, 세리프 표
- 한 화면에 다크 카드 두 장
- 이모지를 제목에 넣기(본문 리스트의 이모지는 허용)
- 금색을 본문 글자색으로 쓰기
